This example tests the QDSP6 optimized AES routines 
found in "aes.h".  It runs against a sample set of 
data and compares the encrypted and decrypted output against the
original input.

To run:

make clean
make sim

The simulation will print "*** PASS" if the test passed.


