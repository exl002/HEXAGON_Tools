This example tests the QDSP6 optimized Reed-solomon decoding routines 
found in "rs.h".  It runs against a sample set of 
data and compares the output against precomputed values.

To run:

make clean
make sim

The simulation will print "*** PASS" if the test passed.


