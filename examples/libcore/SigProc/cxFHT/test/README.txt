This example tests the QDSP6 optimized complex FHT routines 
found in "complexFHT.h".  It runs against a sample set of 
data and compares the output against values computed by
a reference routine.

To run:

make clean
make sim

The simulation will print "*** PASS" if the test passed.


