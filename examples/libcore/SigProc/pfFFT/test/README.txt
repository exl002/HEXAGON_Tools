This example tests the QDSP6 optimized 320 and 360 point FFT routines 
found in "fft320_360.h".  It runs against a sample set of 
data and compares the output against precomputed values.

To run:

make clean
make sim

The simulation will print "*** PASS" if the test passed.


